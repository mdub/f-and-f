#! /usr/bin/env ruby

$: << File.expand_path("../../lib", __FILE__)

require "clamp"
require "faith_and_farming/ocr"

Clamp do

  subcommand "page", "page commands" do

    parameter "N", "page number", :attribute_name => :page_index

    def execute
      page.blocks.each do |block|
        puts "\n--- {left #{block.left}, right #{block.right}, top #{block.top}, bottom #{block.bottom}}\n"
        puts block.text
      end
    end

    private

    def page
      FaithAndFarming::OCR::Page.load(page_index)
    end

  end

  subcommand "tree", "display the tree" do

    def execute
      FaithAndFarming::OCR.pages.each do |page|
        puts "\n--- page #{page.page_index} ---"
        page.tree_entries.each do |entry|
          indent = "  " * (entry.level - 1)
          puts "#{entry.level}#{indent}- #{entry.subject.name}"
        end
      end
    end

  end

end
